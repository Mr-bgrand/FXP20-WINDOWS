# FXP20 Middleware Configuration
PORT=4000
HOST=0.0.0.0

# Reader mode: fxp20-jpos (hardware) or mock (testing)
READER_MODE=fxp20-jpos
READER_ID=FXP20-01

# Mock reader settings (only used when READER_MODE=mock)
MOCK_INTERVAL_MS=500
MOCK_TAG_POOL_SIZE=15

LOG_LEVEL=info
